syntax = "proto3";
package proto;

import "google/api/annotations.proto";

service Api {
    rpc Echo(Message) returns (Message) {
        option (google.api.http) = {
            post: "/echo"
            body: "*"
        };
    }
}

message Message {
    string message = 1;
}
